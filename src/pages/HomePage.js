import { Subheader, Counter } from '../components';

export const HomePage = () => {
  return (
    <main>
        <Subheader heading="Word Counter" subHeading="Free online character and word count tool."/>
        <Counter />
    </main>
    
  )
}
